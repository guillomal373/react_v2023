//Operador condicional ternario

const activo = true;

const mensaje =  ( activo ) ? 'Activo' : 'Inactivo';
console.log(mensaje);

//SOLO VERIFICAR SI ES VERDAD SINO RETORNAR FALSO
const mensaje2 = activo && 'Activo';
console.log(mensaje2);
